pipeline {
    agent any
   
    stages {
        stage ('build') {
            steps {
                echo "start the build"
            }
        
        }
        stage ('test') {
            steps {
                echo "testing is done"
            }
        }
}
}
