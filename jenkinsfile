pipeline {
    agent any
    triggers {
        cron('H */4 * * 1-5')
    }
    stages {
            stage ('run') {
                steps {
                    echo "triggered automatically"
                }
            }
    
    
        
    }
}
